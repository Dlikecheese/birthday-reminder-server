<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiaoyi.birthdayreminder.mapper.FeedbackMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tb_feedback (user, content, create_time)
        values (#{user}, #{content}, #{createTime})
    </insert>

    <select id="count" resultType="Long">
        select count(*)
        from tb_feedback
    </select>

    <select id="list" resultType="com.xiaoyi.birthdayreminder.pojo.dto.FeedbackDTO">
        SELECT f.id,
               u.name,
               f.content,
               f.create_time
        FROM tb_feedback f,
             tb_user u
        where f.user = u.id
        ORDER BY create_time DESC
        limit #{start},#{pageSize}
    </select>
</mapper>
